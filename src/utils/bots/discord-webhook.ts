import type { Alert } from '../../types';

export async function sendDiscordWebhookAlert(webhookUrl: string, alert: Alert): Promise<void> {
}

export async function sendDiscordAlert(webhookUrl: string, alert: Alert): Promise<void> {
}
